class Solution {
    public:
    bool isSubsequence(string s, string t) {
        int sLen=s.size(), tLen=t.size();
        int i=0, j=0;
        
        while(i<sLen and j<tLen){
            if(s[i] == t[j]) i++, j++;
            else j++;
        }
        
        return (i==sLen);
    }
};